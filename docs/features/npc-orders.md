The player can give orders to ally NPCs. An order consists of an action with its parameters values. If the prerequisites are met, the NPC executes that action.

Update the character sheet UI component to add an Order button similar to the Action component the player is using to decide actions (re-use code if possible).

see characters system
