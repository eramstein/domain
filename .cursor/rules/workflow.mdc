---
description: Development workflow for Domain hobby project
alwaysApply: true
---

# Workflow

## Scope

Long-running hobby project. Prefer small, complete slices over large refactors.

## When implementing a feature

1. Read the feature document.
2. Read all referenced system documents in `docs/systems/`.
3. Read architecture rules (and UI structure rules when changing `src/ui/`).
4. Create plan.
5. Implement feature.
6. Update impacted systems documents.

## Documentation

- New/updated systems → `docs/systems/`

## Commands & Testing

```bash
npm run dev      # local development
npm run build    # typecheck and production build
npm run preview  # preview production build
```

No unit tests unless requested. Manual verification via demo screen is sufficient.
